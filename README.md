# Fullstack Intro (React + FastAPI + SQLite)

## In Codespaces
- 创建/打开 Codespace 后，会自动启动：
  - Frontend: 5174 (Preview)
  - Backend: 8000

如果没有自动启动：
```bash
docker compose up --build
```

## Local (optional)

```bash
docker compose up --build
```

## Run tests

```bash
pytest -q
```
